{
  "name": "shophour",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "common"
  ],
  "scripts": {
    "dev": "concurrently -c magentaBright,yellowBright,cyanBright -n COMMON,SERVER,CLIENT \"npm run watch --workspace=common\" \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build:common": "npm run build --prefix common",
    "build:server": "npm run build --prefix server",
    "build": "npm run build:common && npm run build:server"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
